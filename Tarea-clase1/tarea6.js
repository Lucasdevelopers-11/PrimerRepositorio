let usuarios = [];
let ingresoUsuario;
for (let i = 0; i <= 5; i++) {
 
    ingresoUsuario = prompt('Ingrese el nombre de Usuario');

     usuarios.push(ingresoUsuario);

     console.log(usuarios);
}